# local_study_partner_ai
A local LLM help to help with studing with RAG

# Set .env environment variables like this:-
NEBIUS_API_KEY=api key
BASE_URL=llm provider url
API_MODEL=llm model
EMBED_MODEL=embedding model

